注意:
EasyProperty.exe为外挂独立程序，非SolidWorks插件。

为方便启动，我写了一个vba宏来调用该外部exe文件，同时将该宏绑定到快捷键上，这样就可以快速打开EasyProperty.exe了。

安装方法：
1. 将文件复制到“C:\Program Files\SolidWorks Corp\SolidWorks\Macros\EasyProperty”中；
2. 打开SolidWorks，